<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CareerNestR"
        tools:targetApi="31">

        <!-- FIRST SCREEN / SPLASH -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.CareerNestR">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <!-- AUTHENTICATION -->
        <activity
            android:name=".SignInActivity"
            android:exported="false" />

        <activity
            android:name=".SignUpActivity"
            android:exported="false" />

        <!-- DASHBOARD -->
        <activity
            android:name=".Activity_dashboard"
            android:exported="false" />

        <!-- OTHER ACTIVITIES -->
        <activity
            android:name=".ProfileActivity"
            android:exported="false" />

        <activity
            android:name=".ResumeUploadActivity"
            android:exported="false" />

        <activity
            android:name=".DeadlineRemindersActivity"
            android:exported="false" />

        <activity
            android:name=".HomeActivity"
            android:exported="false" />

        <activity
            android:name=".BrowseActivity"
            android:exported="false" />

        <activity
            android:name=".MyCVActivity"
            android:exported="false" />

        <activity
            android:name=".TrackerActivity"
            android:exported="false" />

        <activity
            android:name=".SkillsActivity"
            android:exported="false" />

    </application>

</manifest>